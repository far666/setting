## tmux
#install tmux
yum install tmux -y

# copy conf file
cp tmux.conf ~/.tmux.conf


## vim
#install bundle
git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle

# copy config file
cp vimrc ~/.vimrc

# install plugin
open any file and run 
:PluginInstall
or exec
vim +PluginInstall +qall

more infomation: https://blog.gtwang.org/linux/vundle-vim-bundle-plugin-manager/

## bashrc
# copy config file
cp git-prompt.sh ~/.git-prompt.sh
cp bashrc ~/.bashrc
#reload config
source ~/.bashrc

## iterm2 setting
open iterm2
find Preferences > General > Preferences
load file `com.googlecode.iterm2.plist`

